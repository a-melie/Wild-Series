{% extends 'base.html.twig' %}

{% block title %}Edit Actor{% endblock %}

{% block body %}
<div class="container text-center  mt-3 p-3">
    <h1>Edit Actor</h1>

    {{ include('actor/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex mt-3">
    {{ include('actor/_delete_form.html.twig') }}
    </div>
</div>
    <a  class="text-success btn btn-warning m-5" href="{{ path('actor_index') }}">Retour à la liste des acteurs</a>
{% endblock %}
