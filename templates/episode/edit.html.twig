{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <div class="container text-center  mt-3 p-3">
        <h1>Edit Episode</h1>
        {{ include('episode/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('episode/_delete_form.html.twig') }}
    </div>
    <a href="{{ path('episode_index') }}" class=" text-success btn btn-warning m-5">back to list</a>

{% endblock %}
