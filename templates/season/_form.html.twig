{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn text-success btn-warning mt-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
